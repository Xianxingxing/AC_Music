在此目录放入 README 中引用的截图即可在 GitHub 上正常预览。

建议文件名（与根目录 `README.md` 中的 `<img src>` 一致）：

| 文件 | 说明 |
|------|------|
| `player_main.png` | 主播放界面 |
| `library_tv.png` | 曲库 / TV 遥控界面（可选） |

导出 PNG 时宽度约 720～1080px 即可，体积不宜过大。
